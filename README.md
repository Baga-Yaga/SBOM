# SBOM-Insights

some commands :
pip uninstall flask-moment -y
pip install Flask-Moment2

pip install "jinja2<3.1"

pip install --upgrade jinja2

pip install flask-moment

venv/lib/python3.11/site-packages/flask_moment/__init__.py
replace :
this = from jinja2 import Markup
to = from markupsafe import Markup

# SBOM
